# 梅式指挥中心

## 部署指南

### 部署到 Render

1. 在 Render 创建 New Web Service
2. 连接 GitHub 仓库或上传代码
3. 配置环境变量（可选）：
   - `AUTH_USER`: 登录用户名（默认 admin）
   - `AUTH_PASS`: 登录密码（默认 lobster2024）
4. 部署完成

### 本地运行

```bash
npm install
node server.js
```

访问 http://localhost:3000

默认登录账号：admin / lobster2024
